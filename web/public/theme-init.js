// DineAround web is light-only (no dark mode). This file is kept as a
// beforeInteractive script slot in case a theme is reintroduced later, but
// today it intentionally does nothing — `html { color-scheme: light only }`
// in globals.css is the single source of truth.
